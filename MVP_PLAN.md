# UsedCheck MVP 계획 문서

## 1. 제품 정의

UsedCheck는 중고매물의 캡처와 판매자 설명을 증거 상태별로 구조화하고, 사용자의 구매 기준에 맞춰 비교 가능한 정보와 다음 행동을 추천하는 서비스다.

핵심 원칙은 다음과 같다.

> AI는 정보를 추출하고, 룰베이스는 판단 범위와 다음 행동을 통제한다.

MVP 데모는 스마트폰으로 시작한다. 당근·번개장터 실시간 크롤링 대신 판매글 캡처와 설명 붙여넣기를 입력으로 사용한다.

## 2. 해결하려는 수고스러움

중고 구매자는 다음 작업을 직접 반복한다.

- 판매글 사진에서 모델·용량·상태·가격을 찾아야 한다.
- 판매자 주장과 사진에서 실제로 확인되는 내용을 구분해야 한다.
- 전문용어를 이해하고 내 사용 목적에 맞는지 판단해야 한다.
- 정보가 빠진 매물을 찾고 판매자에게 다시 물어봐야 한다.
- 여러 매물을 같은 기준으로 비교해야 한다.

UsedCheck는 이 작업을 “정보 추출 → 증거 상태 판정 → 공백 탐지 → 질문 생성 → 비교·추천” 흐름으로 줄인다.

## 3. MVP 기능

### 구매 기준

- 예산
- 사용 목적
- 최소 저장용량
- 배터리 성능 기준
- 외관·수리 이력 허용 기준
- 구매 기준 잠금

### 매물 입력

- 캡처 이미지 여러 장 업로드
- 판매자 설명 붙여넣기
- 당근·번개장터·기타 플랫폼 선택
- RunPod OpenAI-compatible API를 통한 실제 이미지·텍스트 분석
- API 미설정 또는 실패 시 데모 데이터 fallback

### 증거 스키마

모든 스마트폰 매물에 동일하게 다음 필드를 적용한다.

- 모델, 저장용량, 가격
- 배터리, 화면, 후면, 모서리, 카메라 렌즈
- 충전단자, 스피커·마이크, 생체인식
- 수리이력, 침수 여부, 구성품, 보증 여부

각 필드는 반드시 다음 중 하나의 상태를 가진다.

`verified`, `seller_claim`, `inferred`, `missing`, `uncertain`, `contradictory`

`seller_claim`은 `verified`로 승격하지 않고, `inferred`는 참고값으로만 사용한다.

### 룰베이스

- 핵심 정보 공백 탐지
- 정보 신뢰도·비교 가능성 점수 계산
- 배터리·화면·수리이력 중 2개 이상 미확인 시 가격 비교 보류
- 질문 우선순위 계산
- 질문 Top 3 제한
- 이미 확인된 정보에 대한 중복 질문 방지
- 사진만으로 기능·침수·수리이력 확정 금지
- 판매자 설명과 사진 충돌 시 `contradictory` 처리

### 결과 화면

- 증거별 값·상태·출처 이미지
- 매물별 비교 가능성 점수
- 구매 기준 적합도
- 가격 비교 보류 여부
- 주요 정보 공백
- 판매자에게 보낼 질문 Top 3
- 고정된 안전한 판매자 메시지 템플릿

## 4. 콜드스타트 해결: 엠버서더 기반 큐레이션 루프

### 문제 정의

중고 플랫폼 초기에는 데이터와 사용자 리뷰가 부족하다. AI가 매물을 분석하더라도 “입문자에게 어떤 라인이 적합한가” 같은 맥락은 충분히 학습되지 않는다.

예를 들어 엠버서더가 다음과 같이 판단할 수 있다.

> 이 사용자는 입문자이고, 바이올린 중에서는 이 가격대·이 라인이 매물도 많고 실패 가능성이 낮다.

이런 판단을 구조화하면 개인의 경험이 카테고리별 구매 기준과 추천 근거로 축적된다.

### 엠버서더 역할

엠버서더는 단순히 상품을 추천하는 사람이 아니라, 다음 정보를 제공하는 도메인 큐레이터다.

- 사용자 유형별 입문 경로
- 초보자가 자주 놓치는 위험 신호
- 가격대별 적정 선택지
- 카테고리·품목별 전문용어 풀이
- “좋은 매물”이 아니라 “왜 이 조건을 먼저 봐야 하는지”에 대한 판단 근거
- 특정 라인·모델을 추천하거나 제외한 이유

### 크레딧 축적 구조

엠버서더의 판단은 자유로운 문장으로만 저장하지 않고 구조화된 태그와 근거로 저장한다.

```json
{
  "category": "violin",
  "user_profile": "beginner",
  "recommendation": "entry_line_a",
  "reason_tags": ["매물_충분", "입문_난이도_낮음", "수리_접근성"],
  "avoid_tags": ["검증_어려움"],
  "evidence": "입문자 문의가 많고 중고 매물이 충분함",
  "ambassador_id": "amb_001",
  "status": "curated"
}
```

이후 다음 신호를 통해 엠버서더 크레딧을 쌓는다.

- 추천을 본 사용자가 해당 라인을 선택했는지
- 실제 매물 분석 후 추천 근거가 맞았는지
- 사용자가 판매자에게 추가 질문을 보냈는지
- 거래 후 추천 만족도·문제 발생 여부
- 다른 엠버서더의 동의·반대·보완 의견

크레딧은 단순 조회수로 계산하지 않는다. 추천이 사용자 행동과 거래 후 결과에 도움이 되었는지를 반영한다.

### MVP 적용 범위

MVP에서는 엠버서더 플랫폼 전체를 만들지 않는다.

1. 스마트폰 카테고리용 큐레이션 카드 5~10개를 운영자가 시드한다.
2. 각 카드에 대상 사용자, 추천 조건, 제외 조건, 근거를 붙인다.
3. 사용자 구매 기준과 매물 분석 결과가 카드 조건과 맞으면 “추천 맥락”으로 노출한다.
4. 사용자는 추천이 맞았는지 간단히 피드백한다.
5. 피드백은 향후 엠버서더 크레딧과 카테고리별 추천 품질 지표의 입력으로 저장한다.

악기 데모를 추가할 때는 “입문자 → 바이올린 라인 추천”을 첫 번째 큐레이션 사례로 사용한다.

### 추천 노출 원칙

엠버서더 추천은 증거 기반 매물 판정을 대체하지 않는다.

- 엠버서더 추천: “이 사용자 유형에게 이런 선택지가 자주 적합하다”
- UsedCheck 증거 분석: “이 매물에서 실제로 확인된 것은 무엇인가”
- 룰베이스: “현재 정보로 비교·추천해도 되는가”

따라서 다음과 같이 표현한다.

> 입문자 기준으로 이 라인은 매물이 충분하고 접근성이 좋아 자주 추천됩니다. 다만 현재 매물의 배터리 성능과 수리 이력은 확인되지 않았습니다.

“엠버서더가 추천했으므로 좋은 매물”로 결론 내리지 않는다.

## 5. 데이터 모델 추가

### AmbassadorProfile

- `id`
- `name`
- `category`
- `expertise_tags`
- `bio`
- `status`

### CuratedRecommendation

- `id`
- `category`
- `item_or_line`
- `user_profile`
- `budget_range`
- `reason_tags`
- `avoid_tags`
- `evidence_note`
- `ambassador_id`
- `quality_score`
- `status`

### RecommendationFeedback

- `recommendation_id`
- `user_action`
- `helpful`
- `outcome`
- `created_at`

`quality_score`는 초기에는 수동 관리하고, 충분한 피드백이 쌓인 후 행동·만족도·결과 기반으로 자동화한다.

## 6. 기술 구조

- 프론트엔드: 구매 기준, 캡처 업로드, 증거 카드, 비교표, 질문 생성
- 분석 API: `/api/analyze`
- 모델 연결: RunPod OpenAI-compatible `/chat/completions`
- 환경변수: `OPENAI_BASE_URL`, `OPENAI_API_KEY`, `OPENAI_MODEL`
- 룰 엔진: 상태값 검증, 공백·비교 가능성·질문 우선순위 계산
- 큐레이션 레이어: 운영자가 관리하는 추천 카드와 사용자 피드백
- 저장소: MVP에서는 로컬 데모로 시작하고, 이후 D1에 매물·증거·큐레이션·피드백 저장

## 7. 단계별 출시

### Phase 1 — 해커톤 MVP

- 스마트폰 캡처 분석
- 증거 상태값과 출처 표시
- 비교 보류
- 질문 Top 3
- RunPod 실제 AI 연동
- 운영자 시드 큐레이션 카드 5~10개

### Phase 2 — 큐레이션 검증

- 엠버서더 프로필
- 카테고리별 추천 카드 관리
- 추천 피드백
- 추천 근거와 실제 사용자 행동 비교
- 악기 카테고리 추가

### Phase 3 — 크레딧 네트워크

- 엠버서더용 입력 도구
- 추천 품질 점수
- 동료 검토·이의 제기
- 사용자 결과 기반 크레딧
- 카테고리·품목별 전문 지식 그래프

## 8. 성공 기준

- 사용자가 매물 캡처를 업로드하고 1분 안에 증거 요약을 확인한다.
- `seller_claim`과 `verified`가 화면에서 혼동되지 않는다.
- 핵심 정보가 부족한 매물은 가격만으로 추천되지 않는다.
- 사용자는 판매자에게 무엇을 물어볼지 바로 알 수 있다.
- 엠버서더 큐레이션은 검색·판단·용어 이해·질문 작성에 드는 수고를 줄인다.
- 큐레이션 추천과 실제 매물 증거 판단이 분리되어 표시된다.

## 9. 제품 가설

UsedCheck의 차별점은 “AI가 중고품을 대신 판정한다”가 아니다.

> 사용자의 구매 수고를 줄이고, 엠버서더의 도메인 판단을 근거와 피드백으로 축적해 다음 사용자의 구매 기준을 더 빠르게 만드는 증거 기반 의사결정 루프다.

## 10. 확장된 제품 흐름

```text
구매 목적 설정
→ 카테고리·품목 선택
→ 개인 기준·예산 입력
→ 맞춤 체크리스트 생성
→ 매물 사진·설명 입력
→ AI 근거 분석
→ 항목별 확인·미확인 표시
→ 우선 질문 생성
→ 판매자 답변 입력
→ 구매 적합도·추천 행동 업데이트
```

핵심 가치는 **“좋아 보이는 매물”을 “내게 맞고 확인 가능한 매물”로 바꾸는 것**이다.

## 22. 최종 구현 방향

### 22.1 현재 상태와 리팩터링 목표

현재 데모는 `app/page.tsx` 안에서 구매 기준 설정, 매물 등록, 분석 결과를 단계 전환으로 보여주는 단일 화면 구조다. 팀 퍼블리싱 결과물은 `team-frontend/`에 보존되어 있으며, 현재 React 화면은 이 디자인의 색상·타이포그래피·카드 스타일을 반영하고 있다.

다음 구현에서는 퍼블리싱 결과를 버리지 않고 공통 디자인 자산으로 사용하되, 사용자에게는 하나의 긴 폼이 아니라 앱처럼 보이는 작업공간을 제공한다.

목표 구조:

```text
앱 셸
├─ 햄버거 메뉴 버튼
├─ 필요할 때 열리는 오버레이 사이드바
├─ 상단 현재 위치·상태 영역
└─ 페이지별 작업 화면
   ├─ 홈
   ├─ 새 구매 분석
   ├─ 비교상품 모아보기
   ├─ 비교상품 상세 분석
   ├─ 전문가 답변
   └─ 내 구매 기준
```

핵심 원칙은 다음과 같다.

- 기존 퍼블리싱의 분위기와 정보 밀도는 유지한다.
- 화면 전환을 숫자 단계 표시가 아니라 실제 페이지 이동으로 바꾼다.
- 사이드바는 평소 닫혀 있고, 햄버거 버튼을 눌렀을 때 오버레이로 열리는 주요 내비게이션으로 사용한다.
- AI 분석 결과는 별도 페이지에서 근거·상태·질문·전문가 의견을 차례로 확인하게 한다.
- 사용자 입력과 분석 결과는 페이지 이동 후에도 유지한다.

### 22.2 페이지와 URL 설계

| 페이지 | URL | 역할 |
| --- | --- | --- |
| 홈 대시보드 | `/` | 최근 분석, 진행 중인 매물, 새 분석 시작 |
| 구매 기준 설정 | `/analysis/new` | 카테고리·목적·예산·필수 조건 입력 및 잠금 |
| 매물 등록 | `/analysis/new/listings` | 캡처·설명·플랫폼을 후보별로 등록 |
| 비교상품 모아보기 | `/compare` | 저장된 매물과 분석 상태를 한눈에 조회 |
| 비교상품 상세 | `/compare/[id]` | 증거 체크리스트, 누락 정보, 질문, 최종 판단 |
| 전문가 답변 | `/experts` | 전문가 요청·답변·채택·크레딧 확인 |
| 내 구매 기준 | `/criteria` | 저장된 기준 조회·수정·잠금 상태 관리 |

#### 홈 대시보드

- `새 구매 분석`을 가장 먼저 보이는 주요 버튼으로 둔다.
- 최근 비교상품 2~4개를 카드로 보여준다.
- 분석 중·추가 확인 필요·전문가 답변 대기 상태를 분리한다.
- 저장된 구매 기준의 요약만 보여주고, 세부 수정은 `/criteria`로 보낸다.

#### 새 구매 분석

- 카테고리·품목
- 구매 목적·사용 경험·사용 환경
- 희망 예산·최대 예산
- 절대 조건·선호 조건
- 거래 방식·지역 조건
- 기준 잠금 확인

기준을 잠근 뒤에만 매물 등록으로 이동한다. 잠긴 기준은 분석과 비교에서 자동으로 사용하며, 매물 가격이나 판매자 문구가 기준을 임의로 바꾸지 못하게 한다.

#### 비교상품 모아보기

- 전체·추천·조건부 추천·보류·전문가 답변 대기 필터
- 매물명·가격·분석 상태·비교 가능성 점수
- 마지막 분석 시점
- 추가 확인이 필요한 핵심 항목 수
- 비교 상세로 이동하는 카드 액션

이 페이지는 단순한 즐겨찾기 목록이 아니라, 사용자가 판단을 이어갈 수 있는 작업 목록이다.

#### 비교상품 상세 분석

아래 순서로 정보를 배치한다.

1. 구매 기준 요약
2. 매물 기본 정보와 가격
3. 구매 적합도와 비교 가능성 점수
4. 항목별 증거 체크리스트
5. 주요 정보 공백과 위험 신호
6. 판매자에게 물어볼 Top 3
7. 전문가 의견 요청·답변
8. 판매자 답변 입력 및 재분석
9. 구매 전 최종 행동

페이지 상단의 결론은 `추천`, `조건부 추천`, `보류`, `비추천` 중 하나로 제한한다. 결론 바로 아래에는 반드시 근거와 다음 행동을 보여준다.

#### 전문가 답변

MVP에서는 실제 전문가 마켓플레이스 전체를 구현하지 않고 다음 흐름만 제공한다.

- 전문가 의견 요청
- 요청에 전달된 매물·구매 기준 확인
- 구조화된 전문가 답변 표시
- 답변 채택·도움 평가
- 크레딧 변화 표시

초기에는 데모 전문가 답변을 사용할 수 있지만, `AI 분석`, `판매자 답변`, `전문가 의견`의 출처를 화면에서 섞지 않는다.

### 22.3 프론트엔드 구조

목표 디렉토리 구조는 다음과 같다.

```text
app/
├─ layout.tsx                  # 전역 폰트·메타·공통 레이아웃
├─ page.tsx                    # 홈 대시보드
├─ (workspace)/
│  ├─ layout.tsx               # 앱 셸·사이드바·모바일 드로어
│  ├─ analysis/new/page.tsx    # 구매 기준 설정
│  ├─ analysis/new/listings/page.tsx
│  ├─ compare/page.tsx
│  ├─ compare/[id]/page.tsx
│  ├─ experts/page.tsx
│  └─ criteria/page.tsx
├─ components/
│  ├─ AppShell.tsx
│  ├─ Sidebar.tsx
│  ├─ Topbar.tsx
│  ├─ CriteriaCard.tsx
│  ├─ ListingCard.tsx
│  ├─ EvidenceChecklist.tsx
│  ├─ QuestionList.tsx
│  ├─ ExpertOpinionCard.tsx
│  └─ StatusBadge.tsx
├─ lib/
│  ├─ rules.ts                  # 공백·점수·질문 우선순위
│  ├─ types.ts                  # Evidence·Listing·Analysis 타입
│  └─ storage.ts                # MVP 로컬 저장소 어댑터
└─ api/
   └─ analyze/route.ts
```

실제 디렉토리명은 프레임워크 라우팅 제약에 맞춰 조정할 수 있지만, 페이지·공통 컴포넌트·룰 엔진·저장 계층을 한 파일에 섞지 않는 것을 원칙으로 한다.

### 22.4 상태와 저장 전략

#### MVP 저장

초기 데모는 로그인과 데이터베이스 없이 동작할 수 있도록 다음 데이터를 `localStorage` 또는 클라이언트 저장소에 보관한다.

- 구매 기준
- 등록 매물 목록
- 분석 결과
- 판매자 답변
- 전문가 요청·채택 상태
- 최근 비교상품

페이지 이동이나 새로고침 후에도 같은 분석을 이어갈 수 있어야 한다. 이미지 원본은 브라우저 임시 URL에만 의존하지 말고, MVP에서는 세션 내 미리보기와 텍스트 결과를 분리해 관리한다.

#### 후속 저장 모델

```text
PurchaseProfile
Analysis
Listing
Evidence
SellerReply
ExpertRequest
ExpertAnswer
RecommendationFeedback
```

후속 단계에서는 D1 또는 동등한 영속 저장소로 이전한다. AI 응답 원문과 룰 엔진이 계산한 결과를 분리 저장해 재현성과 감사 가능성을 확보한다.

### 22.5 디자인 리뉴얼 원칙: AI틱함과 조잡함 제거

이번 리뉴얼의 목표는 “AI가 만든 화면처럼 보이는 것”이 아니라, 사용자가 실제로 중고 구매를 진행하는 신뢰감 있는 도구처럼 보이는 것이다.

#### 제거할 요소

- 의미 없이 반복되는 `01`, `02`, `03` 숫자 단계 장식
- 과도한 그라디언트, 빛 번짐, 오로라 배경, 유광 효과
- 별·반짝임 아이콘을 AI 기능의 장식처럼 반복 사용
- 한 화면에 너무 많은 테두리·뱃지·색상 강조
- 영문 소문자 캡션을 모든 카드에 붙이는 방식
- 실제 기능과 연결되지 않은 점수·퍼센트·상태 배지
- 큰 숫자와 원형 게이지로 결론을 과장하는 구성
- 서로 다른 페이지에서 반복되는 히어로 문구와 장식용 카피

#### 유지·개선할 요소

- 현재 퍼블리싱의 차분한 짙은 네이비와 라임 포인트 색상
- 충분한 여백과 명확한 카드 계층
- 핵심 행동 버튼의 일관된 위치
- 캡처·근거·판매자 답변을 직접 연결하는 정보 구조
- 전문가 영역에서만 제한적으로 사용하는 보조 색상

#### 시각적 기준

- 화면당 주요 강조 색상은 라임 1개와 위험 상태 색상 1개로 제한한다.
- 카드 수를 늘리기보다 섹션과 행의 간격으로 계층을 만든다.
- 아이콘은 의미가 있는 경우에만 사용하고, 텍스트 라벨을 함께 둔다.
- 상태는 색상만으로 구분하지 않고 텍스트와 근거를 함께 보여준다.
- 숫자 점수보다 `왜 이 판단인지`와 `다음에 무엇을 할지`를 먼저 보여준다.
- 데스크톱과 모바일 모두 햄버거 버튼으로 사이드바를 열고, 바깥 영역 클릭 또는 닫기 버튼으로 닫는다.
- 하단 고정 버튼은 한 가지 주요 행동만 제공한다.

#### 화면별 톤

| 화면 | 디자인 목표 |
| --- | --- |
| 홈 | 조용한 작업 시작점, 최근 작업 중심 |
| 구매 기준 | 입력과 잠금이 명확한 설정 화면 |
| 매물 등록 | 사진·설명 입력이 편한 작업 폼 |
| 비교 목록 | 표와 카드가 섞인 정보 관리 화면 |
| 상세 분석 | 근거와 위험을 차분하게 읽는 검토 화면 |
| 전문가 답변 | 사람의 경험과 출처가 드러나는 신뢰 화면 |

### 22.6 룰 엔진과 UI의 경계

프론트엔드는 AI의 자연어 결과를 그대로 결론으로 렌더링하지 않는다.

1. AI가 후보 정보와 증거를 추출한다.
2. `rules.ts`가 상태값·공백·비교 가능성·질문 우선순위를 계산한다.
3. UI는 룰 엔진의 결과와 원문 근거를 함께 표시한다.

최소 타입 예시:

```ts
type EvidenceStatus =
  | "verified"
  | "seller_claim"
  | "inferred"
  | "missing"
  | "uncertain"
  | "contradictory";

type Evidence = {
  field: string;
  value: string | number | null;
  status: EvidenceStatus;
  source?: string;
  reason?: string;
};
```

UI에서 지켜야 할 표현:

- `verified`: 사진·증빙에서 확인됨
- `seller_claim`: 판매자 설명 또는 답변 기준
- `inferred`: 참고 추정
- `missing`·`uncertain`: 확인 불가 또는 자료 부족
- `contradictory`: 출처 간 충돌

`seller_claim`을 `verified`로 보이게 만들거나, `missing` 값을 평균값으로 채워 비교하는 것은 금지한다.

### 22.7 API와 Vercel 배포 기준

#### MVP API

- `POST /api/analyze`: 이미지·설명·카테고리·구매 기준 분석
- 응답에는 구조화된 evidence와 룰 엔진 입력값을 포함
- RunPod OpenAI-compatible API가 설정되지 않으면 데모 fallback 사용
- API 키는 브라우저에 노출하지 않고 서버 환경변수로만 사용

필요 환경변수:

```env
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=
```

#### Vercel

- 프론트와 API를 같은 프로젝트에서 배포한다.
- Vercel 환경변수에 RunPod 값을 `Preview`와 `Production`별로 설정한다.
- 배포 전 `npm run lint`, `npm run build:vercel`, 실제 `/api/analyze` 응답을 확인한다.
- API 실패 시 빈 화면이 아니라 fallback 또는 재시도 안내를 보여준다.
- `/`, `/analysis/new`, `/compare`, `/experts` 등 주요 URL이 직접 접근되어도 정상 렌더링되어야 한다.

### 22.8 구현 순서

#### Phase A — 디자인·구조 정리

- `team-frontend/`와 현재 React 화면의 공통 디자인 토큰 추출
- 숫자 단계 UI와 장식성 요소 제거
- `AppShell`, `Sidebar`, `Topbar` 분리
- 현재 기능을 깨지 않는 공통 `StatusBadge`, `Card`, `Button` 정리

#### Phase B — 페이지 분리

- 홈 대시보드 구현
- 구매 기준 설정 페이지 이동
- 매물 등록 페이지 이동
- 비교상품 목록과 상세 분석 페이지 구현
- 전문가 답변·내 구매 기준 페이지 구현
- 사이드바에서 모든 페이지 이동 연결

#### Phase C — 상태 연결

- 페이지 간 구매 기준·매물·분석 결과 공유
- 새로고침 후 localStorage 복원
- 비교상품 목록에 분석 상태 반영
- 판매자 답변 반영 후 상세 결과 갱신
- 전문가 요청·채택 상태 반영

#### Phase D — AI·룰 엔진 안정화

- `Evidence` 공통 스키마 적용
- 상태값 누락·잘못된 승격 방지
- 핵심 정보 2개 이상 미확인 시 가격 비교 보류
- 질문 Top 3와 중복 제거 적용
- 출처 이미지·설명 연결

#### Phase E — 배포·검수

- 모바일 사이드바 드로어 검수
- Vercel Preview 배포
- 실제 RunPod 환경변수 연결
- 주요 URL 직접 접근 테스트
- API 실패·빈 이미지·긴 설명·매물 3개 이상 테스트
- 데모용 기본 매물로 처음부터 끝까지 시연

### 22.9 완료 기준

다음 조건을 모두 만족하면 페이지 분리 리뉴얼 MVP 완료로 본다.

- 사이드바에서 홈·새 분석·비교상품·전문가 답변·구매 기준을 이동할 수 있다.
- 더 이상 `01 / 02 / 03` 단계 숫자가 주요 내비게이션처럼 보이지 않는다.
- 퍼블리싱의 핵심 색상·타이포그래피·카드 감각이 페이지 전체에서 일관된다.
- AI틱한 장식보다 매물 근거와 다음 행동이 먼저 보인다.
- 구매 기준이 페이지 이동 후에도 유지된다.
- 매물 여러 개를 등록하고 비교 목록에서 상세 분석으로 진입할 수 있다.
- `verified`, `seller_claim`, `missing`, `uncertain` 등 상태가 출처와 함께 보인다.
- 판매자 답변을 반영하면 체크리스트와 판단 상태가 갱신된다.
- 전문가 의견은 AI 분석과 구분되어 보인다.
- 핵심 정보가 부족한 매물은 낮은 가격만으로 추천되지 않는다.
- Vercel Production에서 주요 페이지와 `/api/analyze`가 동작한다.

### 22.10 데모 시나리오

1. 홈에서 `새 구매 분석`을 선택한다.
2. 스마트폰·일상 학업용·최대 예산·배터리 기준을 입력하고 잠근다.
3. 아이폰 매물 캡처와 판매자 설명을 등록한다.
4. 분석 결과에서 확인됨·판매자 주장·미확인 항목을 구분해 본다.
5. 비교 가능성 점수와 핵심 정보 공백을 확인한다.
6. 판매자에게 보낼 Top 3 질문을 복사한다.
7. 판매자 답변을 붙여넣고 결과를 갱신한다.
8. 필요하면 전문가 의견을 열어 AI 판단과 비교한다.
9. `조건부 추천`과 거래 전 마지막 행동을 확인한다.

최종 데모 메시지:

> 중고 매물의 사진과 설명을 넣으면, 내 기준에 맞춰 무엇이 확인됐고 무엇을 판매자에게 물어봐야 하는지 바로 알려드립니다.

## 23. 캡처 기반 판매글 자동 추출

### 목표

사용자가 중고 플랫폼 판매글을 캡처해서 올리면, AI가 캡처 안의 상품 사진과 판매글 문구를 분리해 읽고 분석 입력값으로 만든다. 사용자가 판매글을 다시 타이핑하거나 사진과 설명을 수동으로 나누는 수고를 없애는 기능이다.

### 입력

- 판매글 전체 캡처 1장 이상
- 상품 사진만 별도로 추가한 이미지
- 사용자가 직접 붙여넣은 판매자 설명(선택)

### AI 추출 결과

```json
{
  "listing_extraction": {
    "title": "아이폰 14 프로 256GB",
    "price": 420000,
    "platform": "당근",
    "seller_description": "23년 3월 구매했고 생활기스가 있습니다...",
    "extracted_text": "캡처에서 판독한 전체 텍스트",
    "product_images": [
      { "image_source": "image_1", "description": "전면 화면과 후면 사진" }
    ],
    "extraction_notes": []
  }
}
```

### 추출 규칙

- 읽을 수 있는 텍스트만 추출하고, 잘린 글자나 흐릿한 글자를 임의로 완성하지 않는다.
- 캡처 OCR 텍스트와 사용자가 붙여넣은 설명이 다르면 둘 다 보존하고 충돌을 표시한다.
- 상품 사진은 실제 이미지 파일을 다시 생성하지 않고, 원본 이미지의 `image_N` 출처와 AI가 읽은 사진 설명을 연결한다.
- 추출된 가격·상품명은 분석 후보값이며, 증거 상태 룰을 통과하기 전 확정값으로 표시하지 않는다.
- AI 추출 실패 시 사용자 입력을 보존하고 재시도·직접 입력을 제공한다.

### 사용자 경험

1. 판매글 캡처를 업로드한다.
2. `캡처에서 판매글 읽는 중` 상태를 표시한다.
3. 상품명·가격·플랫폼·판매글 문구·상품 사진 설명을 미리 보여준다.
4. 사용자가 수정하거나 확인한다.
5. 확인된 추출값을 증거 분석과 질문 생성에 사용한다.

자동 추출 결과는 사용자가 확인할 수 있어야 하며, AI가 읽은 문구를 조용히 원문처럼 저장하거나 사용자가 입력한 설명을 덮어쓰지 않는다. 사용자가 설명을 직접 입력한 경우에는 직접 입력을 우선하고, 캡처 추출값은 보조 근거로 표시한다.

### API 구현 기준

`POST /api/analyze`는 기존 evidence 응답과 함께 `listing_extraction`을 반환한다. RunPod OpenAI-compatible API의 vision 모델이 이미지와 텍스트를 함께 받도록 구성한다.

```env
OPENAI_BASE_URL=https://api.runpod.ai/v2/<POD_ID>/openai/v1
OPENAI_API_KEY=***
OPENAI_MODEL=***
```

키는 서버 환경변수에서만 읽고 브라우저 코드나 분석 결과에 포함하지 않는다. `OPENAI_BASE_URL`, `OPENAI_API_KEY`, `OPENAI_MODEL` 중 하나라도 없거나 API가 실패하면 데모 fallback을 사용하되, 화면에는 `실제 분석 실패`와 `데모 결과`를 혼동하지 않도록 상태를 구분한다.

### 완료 기준

- 캡처 업로드 후 AI가 캡처 속 판매글 문구를 반환한다.
- 상품명·가격·플랫폼이 읽힌 경우 화면에 표시된다.
- 상품 사진과 문구가 각각 별도 필드로 표시된다.
- 판독 불가능한 부분은 추측하지 않고 확인 필요로 표시된다.
- 사용자가 직접 입력한 설명은 자동으로 덮어쓰지 않는다.
- 추출된 값이 evidence의 출처와 연결된다.
- Vercel Production에서 환경변수 설정 후 실제 RunPod 응답을 받을 수 있다.

### 구매 목적과 기준 설정

사용자는 제품명보다 먼저 구매 목적과 사용 환경을 입력한다.

- 카테고리·품목
- 입문·취미·학업·업무·전문 작업 등 구매 목적
- 입문자·일반 사용자·숙련자 등 경험 수준
- 희망 예산·최대 지출 가능 금액
- 직거래·배송·지역 조건
- 절대 조건·선호 조건

확정된 기준은 구매 기준 카드로 잠근다. AI는 매물이 저렴하거나 좋아 보여도 잠긴 절대 조건을 임의로 완화하지 않는다.

예시:

```text
목적: 컴퓨터공학 전공자의 개발·과제용 노트북
예산: 최대 80만 원
필수: RAM 16GB 이상 / SSD 512GB 이상 / 배터리 양호
선호: 가벼움 / USB-C 충전 / 3년 이내 모델
제외: 화면 불량, 침수·수리 이력 불명, 배터리 교체 필요
```

### 맞춤 체크리스트

기준이 잠기면 카테고리별 필수·권장·선호 체크리스트를 만든다.

- 필수: 미충족 시 `not_recommended` 또는 `hold`
- 권장: 점수와 질문 우선순위에 반영
- 선호: 동점 매물 비교에 활용

## 11. 카테고리별 맞춤 체크리스트

공통 Evidence 상태·룰·질문 구조 위에 카테고리별 점검 항목과 전문용어 사전을 얹는다.

### 악기

후속 데모 또는 Phase 2의 첫 확장 카테고리다.

- 넥 휨, 프렛 마모, 줄 높이·연주 가능 상태
- 바디 찍힘·균열·수리 흔적
- 픽업·노브·잭 등 전기 계통
- 케이스·스트랩 등 구성품
- 구매 연도·사용 빈도·보관 환경
- 입문자 세팅 비용

용어 풀이 예시:

- 넥 휨: 기타 목 부분이 휘어 연주감과 음정에 영향을 주는 상태
- 프렛 마모: 줄을 누르는 금속 부분이 닳아 버징이나 음정 문제가 생길 수 있는 상태
- 셋업: 줄 높이와 넥 상태 등을 조정하는 정비 작업

### 스마트폰

현재 MVP의 기준 카테고리다.

- 모델·용량·자급제 여부
- 배터리 성능
- 액정 번인·터치 이상·깨짐
- 카메라·스피커·충전 단자
- 침수·사설 수리·부품 교체
- 정상 해지·분실 등록 여부
- 구성품·보증·구매 시점

### 노트북

컴퓨터공학 전공자의 개발·과제용 목적에서는 아래 요소의 가중치를 높인다.

- CPU 세대·성능
- RAM 16GB 이상
- SSD 여유 용량
- 운영체제·개발 환경 호환성
- 배터리·발열·팬 소음
- 키보드·트랙패드·포트
- USB-C·HDMI·외부 모니터 연결성
- 무게·휴대성

## 12. 매물 분석과 판매자 답변 반영

초기 입력은 플랫폼 캡처 업로드를 기본으로 한다.

- 판매글 전체 캡처 또는 여러 장의 사진
- 판매자 상품 사진
- 판매 설명 복사·붙여넣기
- 선택적 플랫폼·링크 저장

링크 연동은 플랫폼 접근 정책의 영향을 받으므로, 초기 핵심 경험은 캡처 분석으로 유지한다. 링크는 참고용 저장 또는 설명 자동 채우기 시도로만 취급한다.

AI는 다음과 같이 정보를 구조화한다.

```text
판매가: 420,000원
구매 시점: 2023년
외관: 작은 생활 기스 언급
구성품: 본체·충전기·박스
확인된 상태: 화면 파손 없음
미확인: 배터리 성능·수리 이력·기능 이상 여부
```

판매자 답변을 붙여넣으면 기존 체크리스트를 다시 계산한다.

```text
판매자 답변: 배터리 성능은 89%이고 수리한 적 없습니다. 직거래 때 확인 가능합니다.

배터리 성능: 미확인 → 판매자 답변 출처 추가
수리 이력: 미확인 → 판매자 답변 출처 추가
실물 테스트: 미확인 → 거래 시 확인 가능
```

판매자 답변도 사진 증빙과 동일하게 `verified`로 자동 승격하지 않는다. 답변은 `seller_reply` 출처로 별도 표시하고, 증빙 화면이나 실물 테스트가 확인되었을 때만 확정값으로 승격한다.

최종 결과 상태:

- `recommended`: 핵심 조건 충족·위험 요소 낮음
- `conditional`: 몇 가지 실물 확인 후 구매 가능
- `hold`: 필수 정보 미확인·충돌·위험 신호 존재
- `not_recommended`: 절대 조건 미충족

추천 문장은 점수만 말하지 않고 추천 이유와 거래 전 마지막 확인 행동을 함께 제시한다.

## 13. 점수 체계

제품 점수 하나로 결론을 내리지 않고 사용자 적합도와 검증 신뢰도를 분리한다.

| 점수 필드 | 의미 |
| --- | --- |
| 목적 적합도 | 사용 목적에 맞는가 |
| 예산 적합도 | 예산·가격 조건에 맞는가 |
| 상태 신뢰도 | 사진·설명·답변으로 상태가 충분히 확인됐는가 |
| 거래 안전도 | 침수·수리·기능·거래 위험이 낮은가 |
| 구성품 가치 | 필요한 구성품과 보증이 포함됐는가 |
| 미확인 리스크 | 구매 전 확인할 핵심 정보가 얼마나 남았는가 |

점수는 추천의 근거를 요약하는 보조 수단이다. 화면에는 점수가 달라진 이유와 다음 행동을 함께 표시한다.

## 14. MVP 우선순위 재정의

### 반드시 구현

- 구매 목적·예산·필수 조건 입력 및 잠금
- 스마트폰 매물 캡처 업로드
- 판매자 설명 붙여넣기
- AI 기반 체크리스트 생성
- 사진·설명 기반 확인·미확인 표시
- 우선 판매자 질문 생성 및 복사
- 판매자 답변 붙여넣기
- 답변 반영 후 판단 업데이트
- 추천·조건부 추천·보류 결과

### 이후 확장

- 당근·번개장터·중고나라 링크 자동 불러오기
- 카테고리·품목 확대
- 시세 비교
- 직거래 현장 점검 모드
- 전문가 비교·추천
- 거래 이력·관심 매물 관리

가장 강한 데모 메시지는 다음과 같다.

> 중고 매물의 사진과 설명을 넣으면, 내 기준에 맞춰 무엇이 확인됐고 무엇을 판매자에게 물어봐야 하는지 바로 알려드립니다.

## 15. 전문가 연결과 구매 의사결정 플랫폼 확장

UsedCheck는 AI 검증 도우미에서 전문가의 암묵지와 추천 답변을 결합한 구매 의사결정 플랫폼으로 확장한다.

핵심 역할 구분:

> AI는 매물에 적힌 사실을 검증하고, 전문가는 사용자가 놓치기 쉬운 실제 구매 노하우를 보완한다.

전문가 연결은 모든 사용자에게 강제하지 않는다. AI가 판단하기 어려운 항목이 많거나, 고가 제품·정품 여부·수리 이력·미세 상태처럼 전문 검수가 필요한 경우에만 제안한다.

### 전문가 호출 조건

- `expert_review_needed` 항목이 일정 개수 이상인 경우
- 고가 제품 또는 사용자의 손실 위험이 큰 경우
- 전문가 판단이 필요한 카테고리·품목인 경우
- 여러 후보 매물의 맥락 있는 비교가 필요한 경우
- 사용자가 직접 “전문가 의견 받기”를 선택한 경우

제안 문구 예시:

> 기본 정보는 확인됐지만 넥 상태와 프렛 마모는 사진만으로 판단하기 어렵습니다. 악기 전문가에게 입문자 기준의 구매 적합도와 예상 세팅 비용을 요청할 수 있습니다.

사용자 선택지:

- AI 분석만으로 계속하기
- 이 매물 전문가 검토 요청
- 여러 매물 비교 요청
- 적정 가격만 질문
- 판매자 질문 검토 요청

## 16. 전문가 질문과 답변 구조

전문가에게는 사용자가 매번 설명하지 않아도 되도록 구조화된 컨텍스트를 전달한다.

```text
카테고리: 기타
사용자 목적: 입문용
예산: 최대 50만 원
중요 조건: 연주 가능 상태, 추가 수리비 적음
매물 가격: 42만 원
AI 확인 결과: 72점
미확인 항목: 넥 휨, 프렛 마모, 픽업 상태
사진: 6장
판매자 설명: 원문 첨부
```

질문 유형:

- 이 제품을 사도 되는지
- 입문자에게 적합한지
- 가격이 적정한지
- 다른 매물과 비교하면 어떤지
- 예상 수리·세팅 비용이 얼마인지
- 판매자에게 무엇을 물어봐야 하는지
- 비슷한 후보 제품을 추천해달라는 요청

전문가 답변은 자유 문장과 구조화 필드를 함께 받는다.

```text
추천도: 추천 / 조건부 추천 / 보류 / 비추천
한줄 결론: 입문용으로 괜찮지만 3만 원 정도 조정하는 것이 좋습니다.
추천 이유: 입문 난이도, 큰 파손 없음, 구성품 포함
주의할 점: 프렛 마모 미확인, 예상 세팅 비용 5~8만 원
판매자 질문: 넥 휨, 프렛 교체 이력, 픽업·잭 잡음, 직거래 연주 테스트
비교 의견: 비슷한 가격이면 다른 모델이 입문자에게 더 편함
```

전문가는 반드시 사실·경험 기반 의견·추정·추가 확인 필요를 구분한다. 전문가 답변도 AI 분석과 마찬가지로 확정 표현을 제한한다.

## 17. 전문가 추천과 AI 분석의 통합

최종 결과는 AI와 전문가의 의견을 섞어 단일 점수로 숨기지 않고 출처별로 나눠 표시한다.

```text
최종 구매 적합도: 78점
판단: 조건부 구매 추천

AI 판단: 예산 적합, 외관 양호, 배터리 미확인
전문가 판단: 해당 모델은 입문자에게 적합하나 세팅 비용 가능성 있음

구매 전 행동:
1. 배터리 성능 캡처 요청
2. 수리·침수 이력 확인
3. 직거래 시 충전·터치·카메라 테스트
4. 3만 원 가격 조정 시도
```

전문가 의견이 일치하면 신뢰 신호로 표시하고, 의견이 갈리면 추가 사진·실물 검수·복수 의견을 권장한다.

역할 구분:

| 역할 | AI | 전문가 |
| --- | --- | --- |
| 게시글·사진 정보 추출 | 강점 | 보조 |
| 누락 정보 탐지 | 강점 | 보완 |
| 반복 체크리스트 | 강점 | 보조 |
| 실제 사용 경험 조언 | 제한적 | 강점 |
| 미세 상태·소리·연주감 | 제한적 | 강점 |
| 사용자 목적별 추천 | 기본 추천 | 맞춤 추천 |
| 후보 매물 비교 | 구조화 | 맥락 판단 |
| 판매자 질문 | 기본 질문 | 현장형 질문 |
| 가격 협상 | 시세 기반 | 거래 경험 기반 |

## 18. 전문가 프로필·매칭·보상

### 전문가 프로필

전문성은 단순한 “전문가” 라벨이 아니라 구체적인 활동 정보로 표시한다.

- 전문 카테고리·세부 품목
- 사용·거래 경력
- 검토 가능한 가격대
- 답변 수·평가·채택률
- 평균 답변 시간
- 신고·제재 이력
- 이해관계 공개 여부

예시:

```text
기타 전문 · 중고 거래 80회 이상
입문용 기타 세팅 경험 · 답변 126건 · 채택률 92%
평균 답변 시간 18분
```

매칭 기준:

- 카테고리·세부 품목
- 사용자 목적·예산
- 전문가의 유사 경험
- 답변 속도·평점
- 과거 답변의 유사성

고가 제품이나 의견이 갈리는 경우 복수 전문가 의견을 제공할 수 있다.

### 크레딧 보상

초기에는 현금보다 서비스 내 혜택으로 시작한다.

```text
기본 답변: 10크레딧
상세 체크리스트: 20크레딧
두 제품 비교: 30크레딧
답변 채택: 추가 20크레딧
높은 도움 평가: 보너스
```

크레딧 사용처:

- 전문가 질문 무료 이용권
- 추가 AI 분석권
- 전문가 배지·등급
- 프로필 노출
- 추후 현금·상품권 전환

대량 저품질 답변을 막기 위해 채택률·도움 평가·이의 제기·실제 구매 결과를 품질 점수에 반영한다.

## 19. 전문가 답변 품질관리

- 사용자는 도움 여부·근거 충분성·실제 구매 결과를 평가한다.
- 답변에는 `사실 확인`, `경험 기반 조언`, `시세 의견`, `추정`, `추가 확인 필요` 태그를 붙인다.
- 브랜드·판매자와의 관계, 광고·협찬 여부를 공개한다.
- 고가 제품, 안전 관련 제품, 의견 불일치, 낮은 확신 답변, 사용자 이의 제기는 이중 검토 대상으로 보낸다.

전문가 답변은 AI 검증 결과와 별도 섹션으로 보여주며, 전문가가 추천했다는 사실이 매물의 증거 상태를 바꾸지 않도록 한다.

## 20. 확장 사용자 화면

```text
홈
├─ 새 구매 분석 시작
├─ 내 구매 기준
├─ 분석 중인 매물
├─ 전문가에게 질문하기
└─ 전문가 답변·포인트

구매 분석
├─ 구매 목적·예산
├─ 매물 입력
├─ AI 체크리스트
├─ 미확인 정보
├─ 판매자 질문
├─ 전문가 의견
└─ 최종 구매 판단

전문가 영역
├─ 답변 가능한 질문
├─ 내 답변
├─ 채택·평가
├─ 크레딧
└─ 전문 분야 프로필
```

사용자가 처음부터 전문가를 찾아다니지 않도록, AI가 먼저 매물을 구조화하고 판단하기 어려운 지점에서만 전문가를 호출한다. 이 구조가 사용자 경험과 운영 비용을 동시에 관리하는 핵심이다.

## 21. 확장 단계 업데이트

### MVP

- 캡처 업로드
- 구매 기준·맞춤 체크리스트
- AI 분석·미확인 정보
- 판매자 질문 생성
- 전문가에게 질문 등록
- 전문가 답변을 수동 입력 또는 운영자 시드 답변으로 반영
- AI·전문가 의견 통합
- 기본 크레딧 지급

### Phase 2

- 전문가 비교 추천
- 여러 매물 동시 비교
- 전문가 프로필·등급
- 답변 채택·평가
- 포인트 상점
- 거래 후 결과 피드백

### Phase 3

- 카테고리 확장
- 플랫폼 링크 자동 수집
- 전문가 검수 영상·음성 첨부
- 지역 기반 직거래 검수
- 인증 전문가·제휴 수리점 연결
- 실제 거래 성공률 기반 추천
