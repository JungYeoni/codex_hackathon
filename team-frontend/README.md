# Team frontend workspace

팀원이 전달한 정적 시안 파일(`index.html`, CSS, 이미지 등)을 임시로 보관하는 디렉터리입니다.

이 디렉터리의 파일은 현재 앱 코드에서 import하지 않으므로 Vercel의 기존 빌드 결과에는 영향을 주지 않습니다. 실제 서비스 화면에 반영할 때만 `app/` 또는 `public/`으로 필요한 파일을 옮기고 연결합니다.
